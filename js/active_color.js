    // "use strict";

// var indicator = document.querySelector('.nav_indicator');
// var items = document.querySelectorAll('.nav_text');

// function handleIndicator(el) {
  // items.forEach(function (item) {
  //   item.classList.remove('is_active');
  //   item.removeAttribute('style');
  // });
  // indicator.style.width = "".concat(el.offsetWidth, "px");
  // indicator.style.left = "".concat(el.offsetLeft, "px");
  // indicator.style.backgroundColor = el.getAttribute('active_color');
  // el.classList.add('is_active');
  // el.style.color = el.getAttribute('active_color');
// }

// items.forEach(function (item, index) {
//   item.addEventListener('click', function (e) {
//     handleIndicator(e.target);
//   });
//   item.classList.contains('is_active') && handleIndicator(item);
// });
     